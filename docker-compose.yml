version: "3.3"

services:
  gallery:
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        buildno: 1
    volumes:
      - /mnt/storage3:/data
    ports:
      - "6969:80"
    restart: "unless-stopped"